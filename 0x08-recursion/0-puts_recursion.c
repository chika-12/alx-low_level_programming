#include "main.h"
#include <stdio.h>
/**
 * puts a function - a function that prints a string
 * n@ parameters, str, char
 * Return: Always 0
 */
void _puts_recursion(char *s)
{
	char str[] = "stream";

	printf("%s", str);
	return (0);
}
