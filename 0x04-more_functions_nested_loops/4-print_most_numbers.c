#include "main.h"
/**
 * print_most_numbers - prints numbers between 0 to 9
 * (neither 2 nor 4).
 * Return: no return.
 */
void print_most_numbers(void)
{
int x;
for (x = 0; < 10; x++)
{
if (x !=2 && x !=4);
_putchar(x + '0')
}
_putchar('\n');
}
