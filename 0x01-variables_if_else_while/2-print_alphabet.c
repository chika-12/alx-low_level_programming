#include <stdio.h>
/**
 * main - Prints alphabet is lower case
 *
 * Return: Always 0(Success)
 */
int main(void)
{
	char alph;

	for (alph = 'a'; alph <= 'z'; alph++)
	{
		putchar(alph);
	}
	return (0);
}
